rrd_opts = function(log_regcheck=TRUE, log_all_err=TRUE, log_all=FALSE) {
  as.list(environment())
}


rrd_md_do_logs = function(project_dir, parcels=list(), outfile="do_log.md") {
  outdir = file.path(project_dir, "reports/do_log")
  outfile =


  outfile = file.path(project_dir, )
}
